<%= form_with(model: account) do |form| %>
  <% if account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
        <% account.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :act_name %>
    <%= form.text_field :act_name %>
  </div>

  <div class="field">
    <%= form.label :act_num %>
    <%= form.number_field :act_num %>
  </div>

  <div class="field">
    <%= form.label :act_type %>
    <%= form.text_field :act_type %>
  </div>

  <div class="field">
    <%= form.label :balance %>
    <%= form.text_field :balance %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
